# Replicable remote downloads — gitignore if files exceed GitHub's 100 MB limit.
# Toggle by commenting/uncommenting the line below.
data/remotes/

# Quarto build cache — regenerated on render, not committed
.quarto/

# R
.Rhistory
.RData
.Rproj.user/

# renv — library is not committed; restore with renv::restore()
renv/library/
renv/staging/

/.quarto/
**/*.quarto_ipynb

# HTML build artifacts from quarto render index.qmd
output/index.html
output/index_files/
